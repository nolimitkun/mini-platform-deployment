apiVersion: v1
kind: Service
metadata:
  name: holmes-a2a
  labels:
    app.kubernetes.io/name: holmes-a2a
    app.kubernetes.io/instance: holmes-a2a
spec:
  selector:
    app.kubernetes.io/name: holmes-a2a
    app.kubernetes.io/instance: holmes-a2a
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
