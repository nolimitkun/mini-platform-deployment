# Open WebUI does OIDC against Keycloak itself and reaches models through
# LiteLLM with the master key, both of which come from Secrets declared on
# earlier releases — so it owns no secret of its own.
routes:
  - name: open-webui
    host: open-webui.test
    service: open-webui
    port: 80
